% ---- acoustic matrix K^0 [isotropic material] ----
\begin{equation}\begin{aligned}
K^0_{00} &= \mathring{\widetilde{\xi}}_0^{2} \left(\lambda^{0} + 2 \mu^{0}\right) + \mathring{\widetilde{\xi}}_1^{2} \mu^{0} + \mathring{\widetilde{\xi}}_2^{2} \mu^{0}\\
K^0_{10} &= \mathring{\widetilde{\xi}}_0 \mathring{\widetilde{\xi}}_1 \left(\lambda^{0} + \mu^{0}\right)\\
K^0_{11} &= \mathring{\widetilde{\xi}}_0^{2} \mu^{0} + \mathring{\widetilde{\xi}}_1^{2} \left(\lambda^{0} + 2 \mu^{0}\right) + \mathring{\widetilde{\xi}}_2^{2} \mu^{0}\\
K^0_{20} &= \mathring{\widetilde{\xi}}_0 \mathring{\widetilde{\xi}}_2 \left(\lambda^{0} + \mu^{0}\right)\\
K^0_{21} &= \mathring{\widetilde{\xi}}_1 \mathring{\widetilde{\xi}}_2 \left(\lambda^{0} + \mu^{0}\right)\\
K^0_{22} &= \mathring{\widetilde{\xi}}_0^{2} \mu^{0} + \mathring{\widetilde{\xi}}_1^{2} \mu^{0} + \mathring{\widetilde{\xi}}_2^{2} \left(\lambda^{0} + 2 \mu^{0}\right)\\
\end{aligned}\end{equation}

